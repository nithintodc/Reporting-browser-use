# Use your local Chrome (persistent profile) so cookies, login state, and 2FA trust are kept.
# Set to true; the script will auto-start Chrome with the profile below if nothing is on 9222.
USE_LOCAL_BROWSER=false
LOCAL_BROWSER_CDP_URL=http://localhost:9222
# Optional: path to Chrome profile dir. If unset, uses .cursor/chrome-debug-profile (persistent).
# Create a profile at chrome://profiles, then use that path so you log in once and reuse.
# CHROME_USER_DATA_DIR=/path/to/your/chrome-profile

# Gmail credentials (only required when USE_LOCAL_BROWSER=false)
GMAIL_EMAIL=
GMAIL_PASSWORD=

# DoorDash merchant portal credentials
DOORDASH_EMAIL=
DOORDASH_PASSWORD=

# Optional: for run_browser_use.py (AI-driven mode). Set one of:
# OPENAI_API_KEY=
# BROWSER_USE_API_KEY=
