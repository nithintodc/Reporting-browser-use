# DoorDash merchant portal credentials (required; vary per account)
DOORDASH_EMAIL=
DOORDASH_PASSWORD=

# Browser Use API (required for main app — native browser-use, no OpenAI)
BROWSER_USE_API_KEY=

# Store IDs are never set here — they come only from combined_analysis sheets
# ("Day-Slot - {StoreID}") for the account you log in with.

# Optional: operator name used in report filenames
# OPERATOR_NAME=

# Optional: push final reports to Google Sheets (google_pusher_agent). Set one of:
# GCP_SERVICE_ACCOUNT_JSON={"type":"service_account",...}   (full JSON string)
# GCP_CREDENTIALS_PATH=/path/to/service-account.json
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
# Or place todc-marketing-*.json in analysis-app/app/

# Optional: for run_browser_use.py (AI-driven mode). Set one of:
# OPENAI_API_KEY=
# BROWSER_USE_API_KEY=
